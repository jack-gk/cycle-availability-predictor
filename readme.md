# Cycle Availability Predictor

An end-to-end data science project that collects TfL cycle availability data, creates bronze, silver and gold data layers, trains a prediction model, and exposes predictions through an API.

## Planned pipeline

TfL API > Bronze data > Silver data > Gold features > Model > FastAPI > Docker > Deployment
